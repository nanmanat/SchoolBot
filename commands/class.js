const { execute } = require("./reactionRoom");

module.exports = {
    name: "class",
    permission: [],
    description: "How many class today",
    async execute(message, args, cmd, client, Discord, profileData){
        
    }
}